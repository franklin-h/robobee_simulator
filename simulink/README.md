# Guide to .slx to .m pairings 
updated_target_driver_2026_withVariants_MPC: 
- has variant for Real (Vicon) and Virtual (Drake/Simulink) setup 
- Tries to have a variant block for MPC as the inner controller, geometric outer controller for desired attitude. 